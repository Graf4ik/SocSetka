let initialState = {
    friends: [
        {id:1, name: 'Dima'},
        {id:2, name: 'Vlad'},
        {id:3, name: 'Sergo'}
      ]
};

const sidebarReducer = (state = initialState, action) => {
    

    return state;
}

export default sidebarReducer;

